version: '3.8'

services:
  backend-green:
    build:
      context: .
      dockerfile: Dockerfile
    image: backend:green
    container_name: backend-green
    ports:
      - "3006:3005"
    env_file:
      - .env
    depends_on:
      - postgres
      - redis
    networks:
      - app-network
    restart: always
    environment:
      - TZ=Asia/Kolkata
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3005/test"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  postgres:
    image: postgres:16
    container_name: postgres-db
    restart: always
    environment:
      POSTGRES_DB: vaidik
      POSTGRES_USER: appuser
      POSTGRES_PASSWORD: vadikVivah
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - app-network

  redis:
    image: redis:7
    container_name: redis
    restart: always
    command: redis-server --requirepass VedVivahRedis123
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data   # ✅ PERSISTENCE FIX
    networks:
      - app-network

networks:
  app-network:
    external: true
    name: vaidik_vivah_backend_app-network

volumes:
  postgres_data:
  redis_data: